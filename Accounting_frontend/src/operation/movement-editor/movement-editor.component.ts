import { Component } from '@angular/core';

@Component({
  selector: 'app-movement-editor',
  templateUrl: './movement-editor.component.html',
  styleUrls: ['./movement-editor.component.scss']
})
export class MovementEditorComponent {
  // this.operationService.set(operation).subscribe((operation) => {
  //   this.formGroup.setValue(operation);
  //   this.notifyService.notify('Сохранено', 'success');
  //   this.dialogRef.close(true);
  // });
}
